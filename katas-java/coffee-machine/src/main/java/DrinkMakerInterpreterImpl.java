public class DrinkMakerInterpreterImpl implements DrinkMakerInterpreter {
    @Override
    public String interpret(CoffeeMachine.DrinkType type, CoffeeMachine.SugarCount sugarCount) {
//        StringBuilder buffer = new StringBuilder();
//        buffer.append(type);
//        buffer.append(":");
//        if (sugarCount > 0) buffer.append(sugarCount);
//        buffer.append(":");
//        if (sugarCount > 0) buffer.append(0);

        return null;
    }
}
